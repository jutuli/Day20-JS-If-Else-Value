function calculation(number) {
  let difference = number - 27;
  let result = difference;
  if (difference > 27) {
    result = difference * 2;
  } else {
    result = result;
  }
  console.log(result);
}

calculation(35);
calculation(74);
calculation(123);
